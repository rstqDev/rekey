[package]
name = "rekey-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Layout tables, language models and the wrong-layout detection engine for Rekey."

[dependencies]
serde = { workspace = true }

[dev-dependencies]
