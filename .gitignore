# Rust
/target
**/target
Cargo.lock.bak

# Node
node_modules
dist
.vite
*.tsbuildinfo

# Tauri
app/src-tauri/target
app/src-tauri/gen

# Python
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Secrets — never commit
.env
.env.*
!.env.example
secrets/

# Build artifacts
/release
*.dmg
*.msi
*.exe.zip

# Model build cache
/tools/.cache

# Trained language models are derived from the corpora in tools/.cache and are
# rebuilt in one command (see README). Committing them would add ~4.6 MB of
# binary churn to history on every retrain.
/crates/rekey-core/data/models/

# Rendered promotional cards, regenerated by automation/make_cards.py
/automation/out/
