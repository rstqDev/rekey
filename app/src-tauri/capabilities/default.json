{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the settings window is allowed to ask the app to do. Deliberately short: this window shows settings and drags itself, and nothing here should grow without a reason.",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
